// Field fixture for the editor's manners. Not compiled by cargo:
// integration tests live at tests/*.rs, never in subdirectories.
// Enter should carry each line's leading whitespace exactly.
// This!

fn main() {
    let items = vec![1, 2, 3];
    for item in items {
        if item > 1 {
            println!("{item}");
            println!("{bringa}");
        }
    }
}
